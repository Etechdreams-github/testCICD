name: Test Self Hosted Runner

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Test runner
        run: |
          echo "CI/CD WORKING"
          whoami
          hostname
          pwd
